(ns bikeGears.test.core
  (:use [bikeGears.core])
  (:use [clojure.test]))

(deftest replace-me ;; FIXME: write
  (print "from test core")
  (is false "No tests have been written.")
  (is (= 1 1 ))  
)
